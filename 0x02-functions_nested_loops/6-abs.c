#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 *@int: check for abs
 *
 * Return: Always 0 (success)
 */
int _abs(int)
{
int x;
if (x < 0)
{
x = x * -1;
_putchar('x' + 48);
}
else
_putchar('x' + 48);
}
