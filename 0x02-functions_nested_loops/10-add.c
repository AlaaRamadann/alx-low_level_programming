#include "main.h"
/**
 *add - function that adds two integers
 *@x: check for number
 *
 *@y: check for number
 *
 *Return: returns the result
 */
int add(int x, int y)
{
int a = x + y;
_putchar(a);
return (a);
}
