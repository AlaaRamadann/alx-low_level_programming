#include "main.h"
/**
 * _strcpy -  function that copies the string
 *
 * @dest: buffer
 * @src: string need to be copied
 *
 * Return: the pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
int i;
for (i = 0; i <=  (src[i] = '\0'); i++)
{
dest[i] = src[i];
}
return (dest);
}	
